{
  "setup-worktree": [
    "npm install",
    "cp $ROOT_WORKTREE_PATH/.env .env 2>/dev/null || cp $ROOT_WORKTREE_PATH/.env.example .env",
    "cp $ROOT_WORKTREE_PATH/amplify_outputs.json amplify_outputs.json 2>/dev/null || true"
  ]
}
