/** Delay before showing skeleton (avoids flash on fast loads). */
export const SKELETON_DELAY_MS = 200;

/** Minimum time to show skeleton once visible (avoids flicker). */
export const SKELETON_MIN_DISPLAY_MS = 300;
