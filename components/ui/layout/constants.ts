/**
 * Breakpoint width (px) below which the app uses the mobile layout
 * (bottom tab bar instead of side NavBar).
 */
export const LAYOUT_BREAKPOINT = 768;

/** Max width for focused detail/form pages on desktop (centered in the content area). */
export const DETAIL_CONTENT_MAX_WIDTH = 960;
