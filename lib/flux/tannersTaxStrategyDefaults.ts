/** Default copy seeds for the Tanner tax strategy calculator block (matches leave-behind footnote tone). */
export const DEFAULT_TANNERS_TAX_STRATEGY_HEADING = 'Your potential tax savings at a glance';

export const DEFAULT_TANNERS_TAX_STRATEGY_DISCLAIMER =
  'Estimates only. Individual results vary. This is not tax or legal advice — consult a qualified tax professional.';
