export const LEADS_EXPORT_SYNC_THRESHOLD = 500;
export const LEADS_EXPORT_CHUNK_SIZE = 500;
/** Must match LEAST(..., 500) cap in saved_lead_list_people_page RPC. */
export const SAVED_LIST_PAGE_MAX = 500;
