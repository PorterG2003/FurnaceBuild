/** Client-side warning threshold for large list membership operations. */
export const LIST_MEMBERSHIP_SYNC_THRESHOLD = 500;

/** Extra heads-up when a view or list operation includes many people (aligns with save-view modal). */
export const LIST_MEMBERSHIP_LARGE_VIEW_THRESHOLD = 5000;
