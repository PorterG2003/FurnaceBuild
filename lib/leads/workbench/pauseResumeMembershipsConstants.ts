/** Sync RPC path up to this many unique global_lead_ids; above uses async job. */
export const PAUSE_RESUME_SYNC_THRESHOLD = 500;

/** Worker chunk size for enrollment action import jobs. */
export const PAUSE_RESUME_IMPORT_CHUNK_SIZE = 500;
