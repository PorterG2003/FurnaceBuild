# Apollo org enrich (~1 credit / company). Required for --live website resolve only.
APOLLO_API_KEY=
# APOLLO_SECRET_TARGET_ENV=dev

# Serper website + site: SOC2 search (~$0.001/search). Required for --live search only.
SERPER_API_KEY=

# Zero-cost fixture mode (no API keys, no live fetch)
USE_FIXTURES=0

MAX_ROWS=
