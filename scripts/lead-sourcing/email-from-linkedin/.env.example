# Apollo.io — required for live enrichment (optional locally; auto-loaded from Amplify SSM)
# APOLLO_SECRET_TARGET_ENV=dev
APOLLO_API_KEY=

# MillionVerifier — used by retry-no-email Pass 3 (optional; auto-loaded from Amplify SSM)
# MILLION_VERIFIER_API_KEY=

# Zero-cost fixture mode (no API key required; uses webinar-hosts fixtures)
USE_FIXTURES=0

# Optional caps
# MAX_APOLLO_CALLS=50
# MAX_ROWS=
