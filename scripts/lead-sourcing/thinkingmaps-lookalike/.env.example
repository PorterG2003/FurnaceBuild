# Zero-cost fixture mode (no live CCD fetch)
USE_FIXTURES=0

MAX_ROWS=

# Optional overrides if the CSVs are not in ~/Downloads
CLOSED_WON_CSV=
AVOID_CSV=

# Paid waterfall (never the default). Required only with --live.
MOLTSETS_API_KEY=
APOLLO_API_KEY=
