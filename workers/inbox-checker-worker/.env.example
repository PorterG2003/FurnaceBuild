# Optional worker-only overrides. Supabase credentials are read from repo-root
# .env.local by default (EXPO_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY).

# Optional: 1-minute recovery tick for local smoke tests (default is 24h in code)
# IMAP_RECOVERY_INTERVAL_MS=60000
