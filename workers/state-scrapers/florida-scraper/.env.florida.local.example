# Copy to .env.florida.local (gitignored) for: docker compose --profile reconciliation up --build
# RUN_MODE is set in docker-compose.yml; do not duplicate unless you override.

JOB_ID=
RECONCILIATION_RUN_ID=
LEADS_SUPABASE_URL=

# Option A — local dev (simplest)
LEADS_SUPABASE_SECRET_KEY=

# Option B — match ECS SSM fetch (mount ~/.aws to /home/pwuser/.aws:ro and set region)
# LEADS_SUPABASE_SECRET_KEY_PARAM_PATH=
# AWS_REGION=us-west-2
